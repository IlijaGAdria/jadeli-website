# Backend-only secrets live here.
# Default local development uses the PostgreSQL container from docker-compose.yml.
# If you prefer Supabase instead, replace DATABASE_URL with your Supabase connection string.

API_PORT=3001
FRONTEND_ORIGIN="http://localhost:3000"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/case_couture"
DIRECTUS_URL="http://localhost:8055"
DIRECTUS_EMAIL="admin@jadeli.com"
DIRECTUS_PASSWORD="change-me"
